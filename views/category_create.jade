extends layout 

block content 
    h1= title 
    form(style="display: flex; flex-direction: column" method="post") 
        label Name: 
            input(type="text" name="name" value=(category === undefined ? "" : category.name) required)
        label Description: 
            input(type="text" name="description" value=(category === undefined ? "" : category.description) required)
        div 
            button(type="submit") Create 


    if errors 
        ul 
            each error in errors 
                li= error.msg